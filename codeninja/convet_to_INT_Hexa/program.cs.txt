#include <bits/stdc++.h> 
string toHex(int num) {
	// Write your code here.
	if(num==0)
	return "0";
	u_int num1=num;
	string res="";
	while(num1){
        int temp=num1%16;
		if(temp<=9)
		res+=48+temp;
		else
		res+=55+temp;
		num1=num1/16;
	}
	reverse(res.begin(),res.end());
	return res;
}